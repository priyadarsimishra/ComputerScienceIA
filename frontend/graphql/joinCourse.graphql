mutation JoinCourse($id: ID!, $courseCode: String!) {
  joinCourse(id: $id, courseCode: $courseCode) {
    username
    email
    courses {
      id
      name
      courseCode
      courseLink
      description
      teacherID
    }
  }
}
